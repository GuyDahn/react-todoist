import { useState, useEffect } from 'react'
import { firebase } from '../firebase'

export const useTasks = selecedProject => {
    const [tasks, setTasks] = useState([])
    useEffect(() => {
         
    }, [])
}